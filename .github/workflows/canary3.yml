name: Canary 3

# Disposable diagnostic - see canary1.yml for the full explanation. Same
# purpose, residue 3 mod 5 offset.
on:
  schedule:
    - cron: "3,8,13,18,23,28,33,38,43,48,53,58 * * * *"
  workflow_dispatch: {}

jobs:
  canary:
    runs-on: ubuntu-latest
    steps:
      - run: echo "canary3 fired at $(date -u +%Y-%m-%dT%H:%M:%S)Z"
