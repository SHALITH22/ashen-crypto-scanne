name: Canary 4

# Disposable diagnostic - see canary1.yml for the full explanation. Same
# purpose, residue 4 mod 5 offset - the last of the 5 possible offsets,
# completing full coverage alongside canary1 (0), canary2 (1), canary3
# (3), and Scan.yml itself (2).
on:
  schedule:
    - cron: "4,9,14,19,24,29,34,39,44,49,54,59 * * * *"
  workflow_dispatch: {}

jobs:
  canary:
    runs-on: ubuntu-latest
    steps:
      - run: echo "canary4 fired at $(date -u +%Y-%m-%dT%H:%M:%S)Z"
